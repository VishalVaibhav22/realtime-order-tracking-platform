VITE_SOCKET_URL=http://localhost:3000

# only needed when there is no dev proxy in front of the api (e.g. a
# production/docker build) - during `npm run dev` this is unset and the
# vite proxy in vite.config.js handles /api requests instead
# VITE_API_URL=http://localhost:3000/api
