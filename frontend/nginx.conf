server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # react router handles routing client-side, so any path that isn't a
    # real file falls back to index.html instead of nginx 404ing it
    location / {
        try_files $uri /index.html;
    }
}
